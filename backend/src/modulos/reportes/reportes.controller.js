import{consolidarLotes,generarExcelMultilote}from'./reportes.service.js';export async function consolidar(req,res){res.json(await consolidarLotes(req.body.lotes))}export async function excel(req,res){const r=await generarExcelMultilote(req.body.lotes);res.setHeader('Content-Type','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');res.setHeader('Content-Disposition',`attachment; filename="${r.nombre}"`);res.send(Buffer.from(r.buffer))}
