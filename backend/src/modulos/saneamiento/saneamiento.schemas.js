import { z } from 'zod';
const item=z.object({item:z.string().trim().min(2).max(300),cumple:z.boolean(),observacion:z.string().trim().max(500).optional().default('')});
export const saneamientoSchema=z.object({tipo:z.enum(['limpieza','desinfeccion','control_plagas','manejo_residuos']),procedimiento:z.string().trim().min(3).max(3000),fecha_programada:z.string().datetime(),fecha_ejecucion:z.string().datetime().nullable().optional(),responsable:z.coerce.number().int().positive().nullable().optional(),resultado:z.string().trim().max(120).nullable().optional(),observaciones:z.string().trim().max(2000).optional().default(''),lista_chequeo:z.array(item).max(100).default([]),estado:z.enum(['programada','ejecutada','cancelada']).default('programada')}).superRefine((d,c)=>{if(d.estado==='ejecutada'&&(!d.fecha_ejecucion||!d.responsable||!d.resultado))c.addIssue({code:z.ZodIssueCode.custom,message:'Una actividad ejecutada requiere fecha, responsable y resultado'});});
